.hero {
  img {
    width: 160px;
  }
}

.text-center {
  text-align: center;
}

.theme-default-content {
  code {
    word-break: break-word;
    border-radius: 2px;
    overflow-x: auto;
    color: #ff502c;
    font-size: .87em;
    padding: .065em .4em;
  }
}

